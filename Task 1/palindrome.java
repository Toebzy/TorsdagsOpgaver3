class palindrome {

    String pal = "makam";
	String pal1= pal.toLowerCase();

	palindrome(String pal) {
	this.pal = pal;
	}

	String ifPalindrome{
	if (pal1.charAt(0)==pal1.charAt(pal1.length()-1) && (pal1.charAt(1)==pal1.charAt(pal1.length()-2)) && (pal1.charAt(2)==pal1.charAt(pal1.length()-3){
	return pal;
	 	
	}
	 
	return false;
	


}
